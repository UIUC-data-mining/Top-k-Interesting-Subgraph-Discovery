/**
 * Java Unit Conversion  Library (jucl)
 * Copyright (c) 2000-2003 jucl team
 *
 * jucl is free software; you can distribute itself under the
 * terms of the BSD-style license received along with the jucl
 * distribution.
 */
package utils.conversions;

/**
 * Utility class containing class operations for converting
 * between SI (or SI derived standard units) and other known
 * units of FlowRate.
 *
 * <br>Note that this class is machine generated, using conversion factors
 * from a text file (in standard Properties format).
 *
 * @author Dieter Wimberger
 * @version 1.0
 */
public class FlowRateConversions {

  /**
   * Utility method converting a double value
   * from lpersec to the SI or SI derived unit.
   *
   * @param d double value to be converted.
   * @return double containing the converted value.
   */
  public static final double lpersec2SI(double d) {
    return d*SI_LPERSEC;
  }//lpersec2SI


  /**
   * Utility method converting a double value
   * from SI or SI derived to lpersec.
   *
   * @param d double value to be converted.
   * @return double containing the converted value.
   */
  public static final double SI2lpersec(double d) {
    return d/SI_LPERSEC;
  }//SI2lpersec


  /**
   * Utility method converting an array of double values
   * from lpersec to the SI or SI derived unit.
   * <br>Note that the array passed in as parameter is the target.
   * The reference to this array is only returned for convenience reasons.
   *
   * @param da Array of double values to be converted.
   * @return Reference to the same array now containing converted values.
   */
  public static final double[] lpersec2SI(double[] da) {
    for (int n = 0; n < da.length; n++) {
      da[n] *= SI_LPERSEC;
    }
    return da;
  }//lpersec2SI


  /**
   * Utility method converting an array of double values
   * from SI or SI derived to lpersec.
   * <br>Note that the array passed in as parameter is the target.
   * The reference to this array is only returned for convenience reasons.
   *
   * @param da Array of double values to be converted.
   * @return Reference to the same array now containing converted values.
   */
  public static final double[] SI2lpersec(double[] da) {
    for (int n = 0; n < da.length; n++) {
      da[n] /= SI_LPERSEC;
    }
    return da;
  }//SI2lpersec


  /**
   * Utility method converting a double value
   * from mmcubicftperd to the SI or SI derived unit.
   *
   * @param d double value to be converted.
   * @return double containing the converted value.
   */
  public static final double mmcubicftperd2SI(double d) {
    return d*SI_MMCUBICFTPERD;
  }//mmcubicftperd2SI


  /**
   * Utility method converting a double value
   * from SI or SI derived to mmcubicftperd.
   *
   * @param d double value to be converted.
   * @return double containing the converted value.
   */
  public static final double SI2mmcubicftperd(double d) {
    return d/SI_MMCUBICFTPERD;
  }//SI2mmcubicftperd


  /**
   * Utility method converting an array of double values
   * from mmcubicftperd to the SI or SI derived unit.
   * <br>Note that the array passed in as parameter is the target.
   * The reference to this array is only returned for convenience reasons.
   *
   * @param da Array of double values to be converted.
   * @return Reference to the same array now containing converted values.
   */
  public static final double[] mmcubicftperd2SI(double[] da) {
    for (int n = 0; n < da.length; n++) {
      da[n] *= SI_MMCUBICFTPERD;
    }
    return da;
  }//mmcubicftperd2SI


  /**
   * Utility method converting an array of double values
   * from SI or SI derived to mmcubicftperd.
   * <br>Note that the array passed in as parameter is the target.
   * The reference to this array is only returned for convenience reasons.
   *
   * @param da Array of double values to be converted.
   * @return Reference to the same array now containing converted values.
   */
  public static final double[] SI2mmcubicftperd(double[] da) {
    for (int n = 0; n < da.length; n++) {
      da[n] /= SI_MMCUBICFTPERD;
    }
    return da;
  }//SI2mmcubicftperd


  /**
   * Utility method converting a double value
   * from cubicmperd to the SI or SI derived unit.
   *
   * @param d double value to be converted.
   * @return double containing the converted value.
   */
  public static final double cubicmperd2SI(double d) {
    return d*SI_CUBICMPERD;
  }//cubicmperd2SI


  /**
   * Utility method converting a double value
   * from SI or SI derived to cubicmperd.
   *
   * @param d double value to be converted.
   * @return double containing the converted value.
   */
  public static final double SI2cubicmperd(double d) {
    return d/SI_CUBICMPERD;
  }//SI2cubicmperd


  /**
   * Utility method converting an array of double values
   * from cubicmperd to the SI or SI derived unit.
   * <br>Note that the array passed in as parameter is the target.
   * The reference to this array is only returned for convenience reasons.
   *
   * @param da Array of double values to be converted.
   * @return Reference to the same array now containing converted values.
   */
  public static final double[] cubicmperd2SI(double[] da) {
    for (int n = 0; n < da.length; n++) {
      da[n] *= SI_CUBICMPERD;
    }
    return da;
  }//cubicmperd2SI


  /**
   * Utility method converting an array of double values
   * from SI or SI derived to cubicmperd.
   * <br>Note that the array passed in as parameter is the target.
   * The reference to this array is only returned for convenience reasons.
   *
   * @param da Array of double values to be converted.
   * @return Reference to the same array now containing converted values.
   */
  public static final double[] SI2cubicmperd(double[] da) {
    for (int n = 0; n < da.length; n++) {
      da[n] /= SI_CUBICMPERD;
    }
    return da;
  }//SI2cubicmperd


  /**
   * Utility method converting a double value
   * from mmcubicftpersec to the SI or SI derived unit.
   *
   * @param d double value to be converted.
   * @return double containing the converted value.
   */
  public static final double mmcubicftpersec2SI(double d) {
    return d*SI_MMCUBICFTPERSEC;
  }//mmcubicftpersec2SI


  /**
   * Utility method converting a double value
   * from SI or SI derived to mmcubicftpersec.
   *
   * @param d double value to be converted.
   * @return double containing the converted value.
   */
  public static final double SI2mmcubicftpersec(double d) {
    return d/SI_MMCUBICFTPERSEC;
  }//SI2mmcubicftpersec


  /**
   * Utility method converting an array of double values
   * from mmcubicftpersec to the SI or SI derived unit.
   * <br>Note that the array passed in as parameter is the target.
   * The reference to this array is only returned for convenience reasons.
   *
   * @param da Array of double values to be converted.
   * @return Reference to the same array now containing converted values.
   */
  public static final double[] mmcubicftpersec2SI(double[] da) {
    for (int n = 0; n < da.length; n++) {
      da[n] *= SI_MMCUBICFTPERSEC;
    }
    return da;
  }//mmcubicftpersec2SI


  /**
   * Utility method converting an array of double values
   * from SI or SI derived to mmcubicftpersec.
   * <br>Note that the array passed in as parameter is the target.
   * The reference to this array is only returned for convenience reasons.
   *
   * @param da Array of double values to be converted.
   * @return Reference to the same array now containing converted values.
   */
  public static final double[] SI2mmcubicftpersec(double[] da) {
    for (int n = 0; n < da.length; n++) {
      da[n] /= SI_MMCUBICFTPERSEC;
    }
    return da;
  }//SI2mmcubicftpersec


  /**
   * Utility method converting a double value
   * from ukgalpermin to the SI or SI derived unit.
   *
   * @param d double value to be converted.
   * @return double containing the converted value.
   */
  public static final double ukgalpermin2SI(double d) {
    return d*SI_UKGALPERMIN;
  }//ukgalpermin2SI


  /**
   * Utility method converting a double value
   * from SI or SI derived to ukgalpermin.
   *
   * @param d double value to be converted.
   * @return double containing the converted value.
   */
  public static final double SI2ukgalpermin(double d) {
    return d/SI_UKGALPERMIN;
  }//SI2ukgalpermin


  /**
   * Utility method converting an array of double values
   * from ukgalpermin to the SI or SI derived unit.
   * <br>Note that the array passed in as parameter is the target.
   * The reference to this array is only returned for convenience reasons.
   *
   * @param da Array of double values to be converted.
   * @return Reference to the same array now containing converted values.
   */
  public static final double[] ukgalpermin2SI(double[] da) {
    for (int n = 0; n < da.length; n++) {
      da[n] *= SI_UKGALPERMIN;
    }
    return da;
  }//ukgalpermin2SI


  /**
   * Utility method converting an array of double values
   * from SI or SI derived to ukgalpermin.
   * <br>Note that the array passed in as parameter is the target.
   * The reference to this array is only returned for convenience reasons.
   *
   * @param da Array of double values to be converted.
   * @return Reference to the same array now containing converted values.
   */
  public static final double[] SI2ukgalpermin(double[] da) {
    for (int n = 0; n < da.length; n++) {
      da[n] /= SI_UKGALPERMIN;
    }
    return da;
  }//SI2ukgalpermin


  /**
   * Utility method converting a double value
   * from mcubicfeetperd to the SI or SI derived unit.
   *
   * @param d double value to be converted.
   * @return double containing the converted value.
   */
  public static final double mcubicfeetperd2SI(double d) {
    return d*SI_MCUBICFEETPERD;
  }//mcubicfeetperd2SI


  /**
   * Utility method converting a double value
   * from SI or SI derived to mcubicfeetperd.
   *
   * @param d double value to be converted.
   * @return double containing the converted value.
   */
  public static final double SI2mcubicfeetperd(double d) {
    return d/SI_MCUBICFEETPERD;
  }//SI2mcubicfeetperd


  /**
   * Utility method converting an array of double values
   * from mcubicfeetperd to the SI or SI derived unit.
   * <br>Note that the array passed in as parameter is the target.
   * The reference to this array is only returned for convenience reasons.
   *
   * @param da Array of double values to be converted.
   * @return Reference to the same array now containing converted values.
   */
  public static final double[] mcubicfeetperd2SI(double[] da) {
    for (int n = 0; n < da.length; n++) {
      da[n] *= SI_MCUBICFEETPERD;
    }
    return da;
  }//mcubicfeetperd2SI


  /**
   * Utility method converting an array of double values
   * from SI or SI derived to mcubicfeetperd.
   * <br>Note that the array passed in as parameter is the target.
   * The reference to this array is only returned for convenience reasons.
   *
   * @param da Array of double values to be converted.
   * @return Reference to the same array now containing converted values.
   */
  public static final double[] SI2mcubicfeetperd(double[] da) {
    for (int n = 0; n < da.length; n++) {
      da[n] /= SI_MCUBICFEETPERD;
    }
    return da;
  }//SI2mcubicfeetperd


  /**
   * Utility method converting a double value
   * from cubicftperd to the SI or SI derived unit.
   *
   * @param d double value to be converted.
   * @return double containing the converted value.
   */
  public static final double cubicftperd2SI(double d) {
    return d*SI_CUBICFTPERD;
  }//cubicftperd2SI


  /**
   * Utility method converting a double value
   * from SI or SI derived to cubicftperd.
   *
   * @param d double value to be converted.
   * @return double containing the converted value.
   */
  public static final double SI2cubicftperd(double d) {
    return d/SI_CUBICFTPERD;
  }//SI2cubicftperd


  /**
   * Utility method converting an array of double values
   * from cubicftperd to the SI or SI derived unit.
   * <br>Note that the array passed in as parameter is the target.
   * The reference to this array is only returned for convenience reasons.
   *
   * @param da Array of double values to be converted.
   * @return Reference to the same array now containing converted values.
   */
  public static final double[] cubicftperd2SI(double[] da) {
    for (int n = 0; n < da.length; n++) {
      da[n] *= SI_CUBICFTPERD;
    }
    return da;
  }//cubicftperd2SI


  /**
   * Utility method converting an array of double values
   * from SI or SI derived to cubicftperd.
   * <br>Note that the array passed in as parameter is the target.
   * The reference to this array is only returned for convenience reasons.
   *
   * @param da Array of double values to be converted.
   * @return Reference to the same array now containing converted values.
   */
  public static final double[] SI2cubicftperd(double[] da) {
    for (int n = 0; n < da.length; n++) {
      da[n] /= SI_CUBICFTPERD;
    }
    return da;
  }//SI2cubicftperd


  /**
   * Utility method converting a double value
   * from mcubicftpersec to the SI or SI derived unit.
   *
   * @param d double value to be converted.
   * @return double containing the converted value.
   */
  public static final double mcubicftpersec2SI(double d) {
    return d*SI_MCUBICFTPERSEC;
  }//mcubicftpersec2SI


  /**
   * Utility method converting a double value
   * from SI or SI derived to mcubicftpersec.
   *
   * @param d double value to be converted.
   * @return double containing the converted value.
   */
  public static final double SI2mcubicftpersec(double d) {
    return d/SI_MCUBICFTPERSEC;
  }//SI2mcubicftpersec


  /**
   * Utility method converting an array of double values
   * from mcubicftpersec to the SI or SI derived unit.
   * <br>Note that the array passed in as parameter is the target.
   * The reference to this array is only returned for convenience reasons.
   *
   * @param da Array of double values to be converted.
   * @return Reference to the same array now containing converted values.
   */
  public static final double[] mcubicftpersec2SI(double[] da) {
    for (int n = 0; n < da.length; n++) {
      da[n] *= SI_MCUBICFTPERSEC;
    }
    return da;
  }//mcubicftpersec2SI


  /**
   * Utility method converting an array of double values
   * from SI or SI derived to mcubicftpersec.
   * <br>Note that the array passed in as parameter is the target.
   * The reference to this array is only returned for convenience reasons.
   *
   * @param da Array of double values to be converted.
   * @return Reference to the same array now containing converted values.
   */
  public static final double[] SI2mcubicftpersec(double[] da) {
    for (int n = 0; n < da.length; n++) {
      da[n] /= SI_MCUBICFTPERSEC;
    }
    return da;
  }//SI2mcubicftpersec


  /**
   * Utility method converting a double value
   * from cubicftpermin to the SI or SI derived unit.
   *
   * @param d double value to be converted.
   * @return double containing the converted value.
   */
  public static final double cubicftpermin2SI(double d) {
    return d*SI_CUBICFTPERMIN;
  }//cubicftpermin2SI


  /**
   * Utility method converting a double value
   * from SI or SI derived to cubicftpermin.
   *
   * @param d double value to be converted.
   * @return double containing the converted value.
   */
  public static final double SI2cubicftpermin(double d) {
    return d/SI_CUBICFTPERMIN;
  }//SI2cubicftpermin


  /**
   * Utility method converting an array of double values
   * from cubicftpermin to the SI or SI derived unit.
   * <br>Note that the array passed in as parameter is the target.
   * The reference to this array is only returned for convenience reasons.
   *
   * @param da Array of double values to be converted.
   * @return Reference to the same array now containing converted values.
   */
  public static final double[] cubicftpermin2SI(double[] da) {
    for (int n = 0; n < da.length; n++) {
      da[n] *= SI_CUBICFTPERMIN;
    }
    return da;
  }//cubicftpermin2SI


  /**
   * Utility method converting an array of double values
   * from SI or SI derived to cubicftpermin.
   * <br>Note that the array passed in as parameter is the target.
   * The reference to this array is only returned for convenience reasons.
   *
   * @param da Array of double values to be converted.
   * @return Reference to the same array now containing converted values.
   */
  public static final double[] SI2cubicftpermin(double[] da) {
    for (int n = 0; n < da.length; n++) {
      da[n] /= SI_CUBICFTPERMIN;
    }
    return da;
  }//SI2cubicftpermin


  /**
   * Utility method converting a double value
   * from bblperd to the SI or SI derived unit.
   *
   * @param d double value to be converted.
   * @return double containing the converted value.
   */
  public static final double bblperd2SI(double d) {
    return d*SI_BBLPERD;
  }//bblperd2SI


  /**
   * Utility method converting a double value
   * from SI or SI derived to bblperd.
   *
   * @param d double value to be converted.
   * @return double containing the converted value.
   */
  public static final double SI2bblperd(double d) {
    return d/SI_BBLPERD;
  }//SI2bblperd


  /**
   * Utility method converting an array of double values
   * from bblperd to the SI or SI derived unit.
   * <br>Note that the array passed in as parameter is the target.
   * The reference to this array is only returned for convenience reasons.
   *
   * @param da Array of double values to be converted.
   * @return Reference to the same array now containing converted values.
   */
  public static final double[] bblperd2SI(double[] da) {
    for (int n = 0; n < da.length; n++) {
      da[n] *= SI_BBLPERD;
    }
    return da;
  }//bblperd2SI


  /**
   * Utility method converting an array of double values
   * from SI or SI derived to bblperd.
   * <br>Note that the array passed in as parameter is the target.
   * The reference to this array is only returned for convenience reasons.
   *
   * @param da Array of double values to be converted.
   * @return Reference to the same array now containing converted values.
   */
  public static final double[] SI2bblperd(double[] da) {
    for (int n = 0; n < da.length; n++) {
      da[n] /= SI_BBLPERD;
    }
    return da;
  }//SI2bblperd


  /**
   * Utility method converting a double value
   * from usgalpermin to the SI or SI derived unit.
   *
   * @param d double value to be converted.
   * @return double containing the converted value.
   */
  public static final double usgalpermin2SI(double d) {
    return d*SI_USGALPERMIN;
  }//usgalpermin2SI


  /**
   * Utility method converting a double value
   * from SI or SI derived to usgalpermin.
   *
   * @param d double value to be converted.
   * @return double containing the converted value.
   */
  public static final double SI2usgalpermin(double d) {
    return d/SI_USGALPERMIN;
  }//SI2usgalpermin


  /**
   * Utility method converting an array of double values
   * from usgalpermin to the SI or SI derived unit.
   * <br>Note that the array passed in as parameter is the target.
   * The reference to this array is only returned for convenience reasons.
   *
   * @param da Array of double values to be converted.
   * @return Reference to the same array now containing converted values.
   */
  public static final double[] usgalpermin2SI(double[] da) {
    for (int n = 0; n < da.length; n++) {
      da[n] *= SI_USGALPERMIN;
    }
    return da;
  }//usgalpermin2SI


  /**
   * Utility method converting an array of double values
   * from SI or SI derived to usgalpermin.
   * <br>Note that the array passed in as parameter is the target.
   * The reference to this array is only returned for convenience reasons.
   *
   * @param da Array of double values to be converted.
   * @return Reference to the same array now containing converted values.
   */
  public static final double[] SI2usgalpermin(double[] da) {
    for (int n = 0; n < da.length; n++) {
      da[n] /= SI_USGALPERMIN;
    }
    return da;
  }//SI2usgalpermin


  /**
   * Utility method converting a double value
   * from cubicftpersec to the SI or SI derived unit.
   *
   * @param d double value to be converted.
   * @return double containing the converted value.
   */
  public static final double cubicftpersec2SI(double d) {
    return d*SI_CUBICFTPERSEC;
  }//cubicftpersec2SI


  /**
   * Utility method converting a double value
   * from SI or SI derived to cubicftpersec.
   *
   * @param d double value to be converted.
   * @return double containing the converted value.
   */
  public static final double SI2cubicftpersec(double d) {
    return d/SI_CUBICFTPERSEC;
  }//SI2cubicftpersec


  /**
   * Utility method converting an array of double values
   * from cubicftpersec to the SI or SI derived unit.
   * <br>Note that the array passed in as parameter is the target.
   * The reference to this array is only returned for convenience reasons.
   *
   * @param da Array of double values to be converted.
   * @return Reference to the same array now containing converted values.
   */
  public static final double[] cubicftpersec2SI(double[] da) {
    for (int n = 0; n < da.length; n++) {
      da[n] *= SI_CUBICFTPERSEC;
    }
    return da;
  }//cubicftpersec2SI


  /**
   * Utility method converting an array of double values
   * from SI or SI derived to cubicftpersec.
   * <br>Note that the array passed in as parameter is the target.
   * The reference to this array is only returned for convenience reasons.
   *
   * @param da Array of double values to be converted.
   * @return Reference to the same array now containing converted values.
   */
  public static final double[] SI2cubicftpersec(double[] da) {
    for (int n = 0; n < da.length; n++) {
      da[n] /= SI_CUBICFTPERSEC;
    }
    return da;
  }//SI2cubicftpersec


  /**
   * Utility method converting a double value
   * from lpermin to the SI or SI derived unit.
   *
   * @param d double value to be converted.
   * @return double containing the converted value.
   */
  public static final double lpermin2SI(double d) {
    return d*SI_LPERMIN;
  }//lpermin2SI


  /**
   * Utility method converting a double value
   * from SI or SI derived to lpermin.
   *
   * @param d double value to be converted.
   * @return double containing the converted value.
   */
  public static final double SI2lpermin(double d) {
    return d/SI_LPERMIN;
  }//SI2lpermin


  /**
   * Utility method converting an array of double values
   * from lpermin to the SI or SI derived unit.
   * <br>Note that the array passed in as parameter is the target.
   * The reference to this array is only returned for convenience reasons.
   *
   * @param da Array of double values to be converted.
   * @return Reference to the same array now containing converted values.
   */
  public static final double[] lpermin2SI(double[] da) {
    for (int n = 0; n < da.length; n++) {
      da[n] *= SI_LPERMIN;
    }
    return da;
  }//lpermin2SI


  /**
   * Utility method converting an array of double values
   * from SI or SI derived to lpermin.
   * <br>Note that the array passed in as parameter is the target.
   * The reference to this array is only returned for convenience reasons.
   *
   * @param da Array of double values to be converted.
   * @return Reference to the same array now containing converted values.
   */
  public static final double[] SI2lpermin(double[] da) {
    for (int n = 0; n < da.length; n++) {
      da[n] /= SI_LPERMIN;
    }
    return da;
  }//SI2lpermin




  /**
   *  Constant conversion factor
   */
   public static final double SI_LPERSEC=0.001;
  /**
   *  Constant conversion factor
   */
   public static final double SI_MMCUBICFTPERD=0.3277413423795;
  /**
   *  Constant conversion factor
   */
   public static final double SI_CUBICMPERD=1.157407407407E-05;
  /**
   *  Constant conversion factor
   */
   public static final double SI_MMCUBICFTPERSEC=28316.84659;
  /**
   *  Constant conversion factor
   */
   public static final double SI_UKGALPERMIN=0.00007576819;
  /**
   *  Constant conversion factor
   */
   public static final double SI_MCUBICFEETPERD=3.277413423795E-04;
  /**
   *  Constant conversion factor
   */
   public static final double SI_CUBICFTPERD=3.277413423795E-07;
  /**
   *  Constant conversion factor
   */
   public static final double SI_MCUBICFTPERSEC=28.31684659;
  /**
   *  Constant conversion factor
   */
   public static final double SI_CUBICFTPERMIN=0.0004719474;
  /**
   *  Constant conversion factor
   */
   public static final double SI_BBLPERD=0.000001840130728;
  /**
   *  Constant conversion factor
   */
   public static final double SI_USGALPERMIN=0.0000630901964;
  /**
   *  Constant conversion factor
   */
   public static final double SI_CUBICFTPERSEC=0.02831684659;
  /**
   *  Constant conversion factor
   */
   public static final double SI_LPERMIN=1.666666666667E-05;


 }//class FlowRateConversions

